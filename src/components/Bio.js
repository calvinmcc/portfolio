import React, { Component } from 'react';

const Bio = () => {
  const bioStyle = {
    backgroundColor: 'white',
    height: '40vh',
    flex: .8
  }

  return (
    <div style={bioStyle} />
  )
}

export default Bio;
